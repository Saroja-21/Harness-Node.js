{
  "name": "nodejs-ci-demo",
  "version": "1.0.0",
  "description": "Sample Node.js app for Harness CI",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'Building application...' && mkdir -p dist && cp -r src/* dist/",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "keywords": ["nodejs", "harness", "ci"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
